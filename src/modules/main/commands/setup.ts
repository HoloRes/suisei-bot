// Imports
import Discord from 'discord.js';

export function run(client: Discord.Client, message: Discord.Message): void {
	// Auto restricts to manage server perms, one time only, sets up all configuration
}

export const config = {
	command: 'setup',
};
