// TODO: This command will allow staff to edit configuration to a limited extent, first to add is the muted role and modlog channel
exports.run = (client, message, args) => {

}

module.exports.config = {
    command: "settings"
}